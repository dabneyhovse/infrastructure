services:
  nginx:
    image: ghcr.io/dabneyhovse/landing
    security_opt:
      - no-new-privileges:true
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.web-nginx.entrypoints=web"
      - "traefik.http.routers.web-nginx.rule=Host(`dabney.house`)"
      - "traefik.http.routers.web-nginx-secure.entrypoints=websecure"
      - "traefik.http.routers.web-nginx-secure.rule=Host(`dabney.house`)"
    networks:
      - traefik
    restart: always

networks:
  traefik:
    external: true